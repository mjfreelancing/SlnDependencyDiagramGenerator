<UserControl
    x:Class="SlnDependencyStudio.Wpf.Controls.FormField"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="FormFieldRoot"
    mc:Ignorable="d">

    <StackPanel>
        <!--  Title  -->
        <TextBlock
            Margin="0,0,0,2"
            FontSize="14"
            FontWeight="Bold"
            Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
            Text="{Binding Title, ElementName=FormFieldRoot}" />

        <!--  Description (collapsed when empty)  -->
        <TextBlock
            x:Name="DescriptionText"
            Margin="0,0,0,6"
            FontSize="12"
            Foreground="{DynamicResource MaterialDesign.Brush.ForegroundLight}"
            Style="{StaticResource CollapseWhenEmptyTextBlockStyle}"
            Text="{Binding Description, ElementName=FormFieldRoot, TargetNullValue=''}"
            TextWrapping="Wrap" />
        <!--  Content (input control)  -->
        <ContentControl
            HorizontalContentAlignment="Left"
            Content="{Binding InputContent, ElementName=FormFieldRoot}"
            Focusable="False" />

        <!--  Validation Error (collapsed when empty)  -->
        <TextBlock
            Margin="0,4,0,0"
            FontSize="12"
            Foreground="{DynamicResource MaterialDesign.Brush.ValidationError}"
            Style="{StaticResource CollapseWhenEmptyTextBlockStyle}"
            Text="{Binding ValidationError, ElementName=FormFieldRoot, TargetNullValue=''}"
            TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
